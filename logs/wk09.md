# Week 07 – Session 16-18 – 3/2 – CSS

## What I learned this week

3/24/26:

1. Where is the <script> tag?
--> At the beginning of the JS portion of the HTML file
2. How does JavaScript find HTML elements on the page?
--> Looking at it's name tag/matches labels you ask for
--> Functions of variables made in the beginning of the code
3. What makes the "Add" button actually do something?
--> JavaScript:
You click Add
Because it’s inside a form, it sends a submit event
JavaScript has a listener: “when submit happens, run this code”
That code takes your text, puts it in the todo list, saves it, and redraws the list on screen
4. Where does the code change the page?
--> The code changes the page when it runs render().
- Erases initial thing shown
- Checks what tasks should be seen
- Creates new list of items
- Puts them onto the page

## What is JavaScript?
- Makes your page do things:
--> Responds to clicks and input, change content dynamically, fetch data from servers, build interactive applications

## What is the DOM?
When the browser loads HTML, it builds a tree in memory called the DOM.
JavaScript can read and change this tree. 

Finding CSS Element with JavaScript:

Use document.querySelector() with ANY CSS SELECTOR.
--> by tag
document.querySelector('h1')
--> by class
document.querySelector('.card')
--> by ID
document.querySelector('#intro')

This returns the first matching element. 

## Events
--> Actions that happen on the page
- User clicks a buton
- User types in an input
- Mouse hovers over an element
- Form is submitted

Style 1: anonymous function (beginner)
button.addEventListener('click', function)

3/26/26:

## Declaring Variables:

Use let for values that change, const for values that stay the same.

let score = 0
score = 10; // OK

const MAX_SCORE = 100;
MAX_SCORE = 200; // Error (WRONG)

## Variable Naming

Rules:
- Start with letter, _, or $
- No spaces or special characters
- Case sensitive

Conventions: camelCase for variables: userName, totalScore
UPPER_CASE for constants: MAX_SIZE, API_KEY

## Primitive Type

Basic data types

// String
    let name = 'Davina';
    let message = `Hello, ${name}! Welcome to JavaScript.`;
    console.log(message);

    const firstName = 'Davina';
    const lastName = 'Li';
    const fullName = `${firstName} ${lastName}`;
    const message = 'Hello, ' + fullName + '! Welcome to JavaScript.';

// Number (integers and decimals)
    let age = 25;
    let price = 19.99;

    const age = 45;
    age = age + 5; <-- This will cause an error because age is a constant

    let age = 45
    age = age + 45 <-- OK

// Boolean
    let isActive = true;
    let isLoggedIn = false;    

// Undefined (no value assigned)
let unknown;

// Null (intentionally empty)
let empty = null;

## Checking Types

Use typeof to check what type a value is. Note: arrays return "object"

typeof "hello" // "string"
typeof 42 // "number"

## String Operations

let first = "hello";
let last = "World";

Concatenation:
first + " " + last // "Hellow World"

## Template Literals
const name = "Alice";
const age = 25;

// Old way (concatenation)
let msg1 = "Hello, " + name + "! You are " + age + ".";

// New way (template literals)
let msg2 = `Hello, ${name}! You are ${age}.`;

// Multi-line strings
let html = `
  <div>
    <h1>${name}</h1>
    <p>Age: ${age}</p>
  </div>
`;

## Arithmetic Operators

Standard math works as expected. % (modulo) gives the remainder, ** is for exponents.

let a = 10, b = 3;

a + b   // 13 (addition)
a - b   // 7  (subtraction)
a * b   // 30 (multiplication)
a / b   // 3.33... (division)
a % b   // 1  (remainder/modulo)
a ** b  // 1000 (exponentiation)

// Shortcuts
a++     // increment by 1
a--     // decrement by 1
a += 5  // same as a = a + 5
a *= 2  // same as a = a * 2


## Comparison Operations: 
Always use === (strict equality) in JS to avoid unexpected type conversions.
--> !== also does the same thing