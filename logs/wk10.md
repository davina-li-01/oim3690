# Week 07 – Session 18-20 – 3/31 – CSS

## What I learned this week

3/31/26:

parseFloat --> Converts string to number

Arithmetic Operator Shortcut:
Know this one: a += 5  // same as a = a + 5

## String Operators:
Common transformation:

let first = "Hello"
let last = "World"

// Concatenation
first + " " + last // "Hellow World"

//Length
first.length // 5

// Mehods
first.toUpperCase() // "HELLO"
first.toLowerCase () // "hello"
first.includes('ell) // true
first.startsWith("He") // true

## Comparison Operators:

5 === "5" // false (strict equality - no conversion)
10 > 5 // true
10 >= 10 // true

## Logical Operators: 

Combine conditions with && (both must be true), || (either can be true), ! (reverse)

What is a function? --> A reusable block of code that performs a task.

// Define function
function greet (){
    console.log("Hello!");
}

// Call function
greet (); // prints "Hello!"
greet (); // prints "Hello!" again

## Functions with Parameters

Parameters let functions work with different inputs each time you call them.

function greet(name) {
    console.log ('Hello, ${name}!');
}

greet ("Alice"); // "Hello, Alice!"
greet ("Bob"); // "Hello, Bob!"

## Return Values
Use return to send a result back. Without it, the function returns undefined.

function add(a, b) {
  return a + b;
}

let sum = add(5, 3);  // sum = 8

// Use the result directly
console.log(add(10, 20));  // 30

// No return = undefined
function sayHi() {
  console.log("Hi");
  // implicitly returns undefined
}

## Arrow Functions

Arrow functions are shorthand. AI-generated codes uses them often, so learn to recognize them.

// Traditional function
function add(a, b) {
  return a + b;
}

// Arrow function
const add = (a, b) => {
  return a + b;
};

// Short form (implicit return)
const add = (a, b) => a + b;

// Single parameter (no parentheses needed)
const double = x => x * 2;

## Conditionals

Conditionals let your code make decisions:

If the user is logged in, show their profile
If the form is invalid, show an error
If the score is high enough, show "You Win!"

Basic if:

let age = 20;

if (age >= 18) {
  console.log("You are an adult"); // code runs if condition is true
}

let age = 15;

Use else when you have exactly 2 paths.

if (age >= 18) {
  console.log("You are an adult");
} else {
  console.log("You are a minor");
}

## if...else if...else

Conditions are checked in order. The first true condition runs and the rest are skipped.

let score = 85;

if (score >= 90) {
  console.log("A");
} else if (score >= 80) {
  console.log("B");
} else if (score >= 70) {
  console.log("C");
} else if (score >= 60) {
  console.log("D");
} else {
  console.log("F");
}

REMEMBER: IT IS CHECKED SEQUENTIALLY!!!